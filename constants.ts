export const VALID_BRANDS: string[] = [
 
];

export const GEMINI_TEXT_MODEL = 'gemini-2.5-flash-preview-04-17';

// Default Base URL for the proxy server
export const PROXY_BASE = ""; 
export const LOCAL_STORAGE_CORS_PROXY_URL_KEY = 'corsProxyUrl'; // Stores the user-defined proxy base URL